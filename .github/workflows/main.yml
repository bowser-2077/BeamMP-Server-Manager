name: Build and Pre-Release

on:
  push:
    branches:
      - main
    paths:
      - "app.pyw"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build executable
        run: |
          pyinstaller --noconsole --onefile --icon=icon.ico app.pyw

      - name: Get short commit SHA
        id: vars
        shell: bash
        run: echo "sha_short=${GITHUB_SHA::7}" >> "$GITHUB_ENV"

      - name: Rename executable
        run: move dist/app.exe dist/BeamMP-Server-Manager.exe

      - name: Create Pre-release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: pre-${{ env.sha_short }}
          name: "Pre-release ${{ env.sha_short }}"
          body: "Auto build for commit ${{ env.sha_short }}"
          draft: false
          prerelease: true
          files: dist/BeamMP-Server-Manager.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
